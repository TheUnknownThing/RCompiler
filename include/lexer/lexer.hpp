#pragma once

#include <cctype>
#include <iostream>
#include <string>
#include <string_view>
#include <unordered_map>
#include <vector>

namespace rc {

// --- GENERATED By Google Gemini ---
#define X(name, str) name,
enum class TokenType {
#include "token_defs.def"
};
#undef X

const char *to_string(TokenType type);

std::ostream &operator<<(std::ostream &os, const TokenType &type);

// --- END GENERATED By Google Gemini ---

extern const std::unordered_map<std::string_view, TokenType>
    keyword_to_token_type;

extern const std::unordered_map<std::string_view, TokenType>
    operator_to_token_type;

extern const std::unordered_map<std::string_view, TokenType>
    punctuation_to_token_type;

struct Token {
  TokenType type;
  std::string lexeme;
};

class Lexer {
public:
  Lexer(const std::string &source);
  std::vector<Token> tokenize();

private:
  std::string source;
  std::vector<Token> tokens;

  void first_pass();
  bool check_word_boundary(char c);
  bool is_punctuation(char c);
  void check_for_keywords();
};







} // namespace rc
